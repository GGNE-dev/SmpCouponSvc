global:
  scrape_interval: 5s # 5초마다 데이터를 긁어갑니다.

scrape_configs:
  - job_name: 'smp-coupon-svc'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
