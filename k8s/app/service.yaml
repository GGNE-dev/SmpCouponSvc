apiVersion: v1
kind: Service
metadata:
  name: smp-coupon-svc
  namespace: smp-coupon
  labels:
    app: smp-coupon-svc
spec:
  type: NodePort
  selector:
    app: smp-coupon-svc
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
    nodePort: 30080
