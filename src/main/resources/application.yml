spring:
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, metrics
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: smp-coupon-svc
